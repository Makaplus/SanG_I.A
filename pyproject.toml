[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "sang-ia"
version = "0.1.0"
description = "SANGIA - organizzazione documenti giudiziari"
readme = "README.md"
requires-python = ">=3.10"
